/* import login from 'login'
import store from 'store'
import Vue from 'vue'
import Vuex from 'vuex'

export default new Vuex.Store({
    modules: {
        login: login,
        store: store
    }
}) */
